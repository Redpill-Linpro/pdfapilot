server:
    port: 8083

spring:
    profiles:
        active: dev
    data:
        mongodb:
            # host: localhost
            # port: 27017
            host: 172.16.1.2
            port: 27017
            database: pdfaPilotPromus

    mail:
        baseUrl: http://localhost:8080

    thymeleaf:
        mode: XHTML
        cache: false

metrics:
    jmx.enabled: true
    spark:
        enabled: false
        host: localhost
        port: 9999
    graphite:
        enabled: false
        host: localhost
        port: 2003
        prefix: pdfaPilotPromus
